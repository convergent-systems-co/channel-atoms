id           = "channel-atoms/protocol/redis"
version      = "1.0.0"
content_hash = ""
lifecycle    = "draft"
created_at   = "2026-05-26T00:00:00Z"

[body]
name          = "Redis"
description   = "Redis Serialization Protocol (RESP) — in-memory key-value and pub/sub messaging over TCP"
wire_protocol = "tcp"
default_port  = 6379
encryption    = false
stateful      = true

[meta]
provenance = "https://redis.io/docs/reference/protocol-spec/"
license    = "Apache-2.0"
